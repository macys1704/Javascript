import prompt from 'prompt-sync';
let ler = prompt();

console.log('Programa para calcular a area de um de quadrado \n Me fala o tamanho do lado em metros para eu calcular a área:')

let n1 = Number(ler());
let r = n1 * n1;

console.log('A área desse quadrado é ' = r);